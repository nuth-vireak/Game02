#pragma once

class SwagBall
{
private:

public:
	// Constructor and Destructors
	//--------------------------------------------------------------------------------------------------------------------
	SwagBall();													// Constructors
	virtual ~SwagBall();										// Virtual Destructors
};

